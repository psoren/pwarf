import { describe, it } from "vitest";

describe("haunting (removed)", () => {
  it("system removed from pipelines", () => {
    // Haunting system removed — ghosts no longer stress living dwarves.
  });
});
